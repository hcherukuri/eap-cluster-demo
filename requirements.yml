---
# Install from Red Hat Ansible Automation Hub (requires authenticated access).
#   ansible-galaxy collection install -r requirements.yml
#
# The redhat.eap collection is not published on the public Galaxy server.
collections:
  - name: redhat.eap
    version: ">=1.5.11"
